spring:
  cloud:
    kubernetes:
      client:
        namespace: carloszamo077-dev
  application:
    name: simplecalculator

simplecalculator:
  ribbon:
    eureka:
      enabled: false
    client:
      enabled: true
    ServerListRefreshInterval: 5000
ribbon:
  http:
    client:
      enabled: true